# -------- SYSTEM --------
N = 400
boxSize = 20
dt = 0.0005        // Ne pas dépasser ~0.001 pour LJ (risque de sauter les barrières énergétiques)
steps = 15000
initVelocity = 0.3

# -------- POTENTIAL --------
potential = LJ     // "LJ" (Lennard-Jones) ou "NONE" (gaz parfait)
# potential = NONE

# -------- INTEGRATOR --------
integrator = VV    // "VV" (Velocity Verlet), "Euler", "RK4"
# integrator = Euler
# integrator = RK4

# -------- LENNARD-JONES --------
epsilon = 1.0      // profondeur du puits : V(r) = 4ε[(σ/r)¹² - (σ/r)⁶]
sigma   = 1.0      // distance à laquelle V = 0 ; r_min = 2^(1/6)σ ≈ 1.122
cutoff  = 2.5      // rayon de troncature en unités absolues

# -------- RDF --------
rdfBins  = 120     // résolution de g(r)
rdfStart = 3000    // step à partir duquel on échantillonne (système supposé thermalisé)